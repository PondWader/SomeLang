DEFINE FUNCTION execute(ast: Node[], fileName: string, globals: map[string]any, modules: map[string]map[string]any):
	SET env TO new Environment(null, new Call(fileName, 0, "main"))

	START LOOP FOR name, val IN globals:
		env.Set(name, val)
	END LOOP

	CALL env.Execute(ast)
